#define alpha 1.0
@kernel void axpy(const int N, @restrict const float* x, @restrict float* y, @restrict float* ans) {
  @outer for (int o=0; o < N; o+= 1024) {
    @inner for (int i = 0; i < 1024; ++i) {
      ans[i+o] = alpha * x[i+o] + y[i+o];
    }
  }
}
